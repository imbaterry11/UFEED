% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/UFEED_feature_expansion_helpers.R
\name{UFEED_compute_atmospheric_demand_features}
\alias{UFEED_compute_atmospheric_demand_features}
\title{Compute atmospheric evaporative-demand features}
\usage{
UFEED_compute_atmospheric_demand_features(
  ufeed_data,
  esat_formula = c("Allen_1998", "Sonntag_1990", "Alduchov_1996"),
  clamp_negative_vpd = TRUE
)
}
\arguments{
\item{ufeed_data}{A data frame containing UFEED raw weather columns.}

\item{esat_formula}{Saturation-vapor-pressure formulation passed to
\code{bigleaf}. The default, \code{"Allen_1998"}, is consistent with FAO-56.}

\item{clamp_negative_vpd}{Logical. Replace small negative VPD values caused
by inconsistent daily summaries or numerical noise with zero.}
}
\value{
The input data frame with atmospheric-demand features appended.
}
\description{
Adds daily atmospheric moisture-demand variables using functions from the
\code{bigleaf} package. Actual vapor pressure is calculated preferentially from
daily dew-point temperature (\code{T2MDEW}). If dew point is missing for a row,
relative humidity (\code{RH2M}) is used as a fallback.
}
\details{
The daily maximum and minimum VPD variables are proxies because UFEED has
daily temperature extrema and daily mean dew point rather than simultaneous
hourly temperature and humidity observations.

Required input:
\itemize{
\item \code{T2M}
}

At least one humidity input is required:
\itemize{
\item \code{T2MDEW}, or
\item \code{RH2M}
}

Optional inputs:
\itemize{
\item \code{T2M_MAX} for \code{VPD_MAX_PROXY}
\item \code{T2M_MIN} for \code{VPD_MIN_PROXY}
\item \code{PS} for \code{SPECIFIC_HUMIDITY}
\item \code{WS2M} for \code{VPD_WIND}
}

Generated variables and units:
\itemize{
\item \code{ESAT_T2M}: saturation vapor pressure at mean temperature, kPa
\item \code{ESAT_T2M_MAX}: saturation vapor pressure at maximum temperature, kPa
\item \code{ESAT_T2M_MIN}: saturation vapor pressure at minimum temperature, kPa
\item \code{EA}: actual vapor pressure, kPa
\item \code{VPD}: vapor pressure deficit at mean temperature, kPa
\item \code{VPD_MAX_PROXY}: VPD proxy using Tmax and daily mean dew point/RH, kPa
\item \code{VPD_MIN_PROXY}: VPD proxy using Tmin and daily mean dew point/RH, kPa
\item \code{DEWPOINT_DEPRESSION}: T2M minus T2MDEW, degree C
\item \code{SPECIFIC_HUMIDITY}: specific humidity, kg kg-1
\item \code{VPD_WIND}: VPD multiplied by mean wind speed, kPa m s-1
}
}
