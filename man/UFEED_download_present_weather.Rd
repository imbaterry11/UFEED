% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/UFEED_core_functions.R
\name{UFEED_download_present_weather}
\alias{UFEED_download_present_weather}
\title{Download present-season daily weather data}
\usage{
UFEED_download_present_weather(
  lon,
  lat,
  weather_data_source = c("power", "power_ee", "power_open_meteo"),
  parameters = NULL,
  pairwise = TRUE,
  past_days = 9,
  forecast_days = 8,
  weather_model = "era5_seamless",
  soil_model = "best_match",
  soil_na_lon_threshold = -50,
  soil_aggregation = "daily_mean",
  max_retries = 10,
  retry_wait_sec = 2,
  return_components = FALSE
)
}
\arguments{
\item{lon}{Numeric longitude.}

\item{lat}{Numeric latitude.}

\item{weather_data_source}{Weather source for the historical backbone. One of
\code{"power"}, \code{"power_ee"}, or \code{"power_open_meteo"}.}

\item{parameters}{Character vector of weather variables to request. If \code{NULL},
UFEED present defaults are used.}

\item{pairwise}{Logical. If \code{TRUE}, pair \code{lon[i]} with \code{lat[i]}. If \code{FALSE},
use all longitude-latitude combinations.}

\item{past_days}{Number of past days to request from Open-Meteo.}

\item{forecast_days}{Number of forecast days to request from Open-Meteo.}

\item{weather_model}{Open-Meteo model name.}

\item{soil_model}{Open-Meteo soil fallback model name.}

\item{soil_na_lon_threshold}{Longitude threshold used to trigger Open-Meteo
soil fallback for recent/forecast weather.}

\item{soil_aggregation}{Soil fallback aggregation method.}

\item{max_retries}{Maximum number of download attempts per site.}

\item{retry_wait_sec}{Seconds to wait between retry attempts.}

\item{return_components}{Logical. If \code{TRUE}, return historical, recent/forecast,
and combined weather tables.}
}
\value{
A daily weather data frame, or a list of data frames if
\code{return_components = TRUE}.
}
\description{
This exposes the weather download step used internally by \code{UFEED_present()}.
It downloads a historical backbone beginning from the previous dormant-season
start so that cumulative dormant-season features can be computed correctly,
then overlays recent and forecast Open-Meteo data.
}
\details{
For Northern Hemisphere sites, the historical backbone starts on September 1
of the previous year. For Southern Hemisphere sites, UFEED uses the matching
shifted seasonal calendar and starts on March 1 of the previous year.
}
