% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/UFEED_core_functions.R
\name{UFEED_compute_weather_features}
\alias{UFEED_compute_weather_features}
\title{Compute UFEED weather-derived features}
\usage{
UFEED_compute_weather_features(
  weather_data,
  feature_profile = c("auto", "history", "present"),
  included_module = c("cumsum_features", "EWMA_REWMA_features",
    "cumulative_temp_features", "season_summary_features"),
  cumsum_cols = "default",
  ewma_rewma_cols = "default",
  cumsum_max_missing_prop = 0.1,
  ewma_rewma_windows = c(2, 3, 4, 5, 6, 7, 10, 14, 21, 30, 45, 60, 90),
  ewma_rewma_max_missing_prop = 0.5,
  ewma_rewma_require_full_window = TRUE,
  cumulative_temp_rollsum_windows = c(3, 7, 14, 30, 60, 90),
  cumulative_temp_chilling_models = c("CU", "Utah", "NC", "DP"),
  cumulative_temp_gdh_bases = c(10, 7, 4, 0),
  cumulative_temp_gdd_bases = c(0, 4, 7, 10),
  cumulative_temp_gdh_Topt = 25,
  cumulative_temp_gdh_Tcrit = 36,
  cumulative_temp_fill_direction = "downup",
  cumulative_temp_message_missing = TRUE,
  season_max_cols = "default",
  season_min_cols = "default",
  start_filter_date = NULL,
  end_filter_date = NULL,
  message_progress = TRUE
)
}
\arguments{
\item{weather_data}{Data frame containing at least \code{Date}, \code{lon}, and \code{lat}.
Required weather columns depend on the selected modules.}

\item{feature_profile}{Character. One of \code{"auto"}, \code{"history"}, or \code{"present"}.
\code{"auto"} infers the profile from available columns. Use \code{"history"} for data
from \code{UFEED_download_history_weather()} and \code{"present"} for data from
\code{UFEED_download_present_weather()} when you want to be explicit.}

\item{included_module}{Character vector of feature modules to compute. Valid
values are \code{"cumsum_features"}, \code{"EWMA_REWMA_features"},
\code{"cumulative_temp_features"}, and \code{"season_summary_features"}.
The default computes all four modules. Users may provide any subset of
these module names. Any other value causes an error.}

\item{cumsum_cols}{Character vector or \code{"default"}. Columns used for
cumulative-sum features. With \code{"default"}, the valid column set depends on
\code{feature_profile} and is automatically restricted to columns present in
\code{weather_data}. For \code{feature_profile = "history"}, valid choices are
\code{"ALLSKY_SFC_SW_DWN"}, \code{"ALLSKY_SFC_LW_DWN"},
\code{"ALLSKY_SFC_PAR_TOT"}, and \code{"PRECTOTCORR"}. For
\code{feature_profile = "present"}, valid choices are
\code{"ALLSKY_SFC_SW_DWN"} and \code{"PRECTOTCORR"}. If users provide a
character vector, every value must be a valid choice for the selected
profile and must be present in \code{weather_data}; otherwise the function stops
with an error.}

\item{ewma_rewma_cols}{Character vector or \code{"default"}. Columns used for
EWMA and reverse-EWMA features. With \code{"default"}, the valid column set
depends on \code{feature_profile} and is automatically restricted to columns
present in \code{weather_data}. For \code{feature_profile = "history"}, valid
choices are \code{"T2M"}, \code{"T2M_MAX"}, \code{"T2M_MIN"}, \code{"T2MDEW"},
\code{"Daily_Temp_Fluctuation"}, \code{"ALLSKY_SFC_SW_DWN"},
\code{"ALLSKY_SFC_LW_DWN"}, \code{"ALLSKY_SFC_PAR_TOT"}, \code{"PRECTOTCORR"},
\code{"RH2M"}, \code{"WS2M"}, \code{"WD2M"}, \code{"WS2M_MAX"},
\code{"WS2M_MIN"}, \code{"PS"}, \code{"GWETROOT"}, \code{"GWETTOP"},
\code{"TSOIL1"}, \code{"TSOIL3"}, \code{"EVPTRNS"}, and \code{"CLOUD_AMT"}.
For \code{feature_profile = "present"}, valid choices are the same except
\code{"ALLSKY_SFC_LW_DWN"}, \code{"ALLSKY_SFC_PAR_TOT"}, and \code{"CLOUD_AMT"}
are not valid because they are not downloaded in the standard present-
weather workflow. If users provide a character vector, every value must be
a valid choice for the selected profile and must be present in
\code{weather_data}; otherwise the function stops with an error.}

\item{cumsum_max_missing_prop}{Numeric between 0 and 1. Maximum missing-data
proportion allowed for cumulative-sum features.}

\item{ewma_rewma_windows}{Integer vector. EWMA/REWMA window lengths.
The UFEED default is \code{c(2, 3, 4, 5, 6, 7, 10, 14, 21, 30, 45, 60, 90)}.
Users may provide any integer vector with values greater than or equal to 2,
such as \code{c(7, 14, 30)} or \code{c(5, 15, 45, 120)}. The value \code{1} is not
allowed because a one-day EWMA/REWMA window does not provide smoothing.}

\item{ewma_rewma_max_missing_prop}{Numeric between 0 and 1. Maximum
missing-data proportion allowed inside each EWMA/REWMA window.}

\item{ewma_rewma_require_full_window}{Logical. If \code{TRUE}, EWMA/REWMA
features are returned only when the full requested backward-looking window
is available. This avoids early rows where \verb{*_EWMA_90} or \verb{*_REWMA_90}
would otherwise be computed from fewer than 90 days.}

\item{cumulative_temp_rollsum_windows}{Integer vector. Rolling-sum windows
for cumulative temperature features. The UFEED default is
\code{c(3, 7, 14, 30, 60, 90)}. Users may provide any positive-integer vector;
it does not need to be a subset of the default.}

\item{cumulative_temp_chilling_models}{Character vector. Chilling models to
compute. Valid choices are \code{"CU"}, \code{"Utah"}, \code{"NC"}, and
\code{"DP"}. Any other value causes an error.}

\item{cumulative_temp_gdh_bases}{Integer vector. Base temperatures for GDH
features. The UFEED default is \code{c(10, 7, 4, 0)}. Users may provide any
finite integer vector; it does not need to be a subset of the default. Use
\code{integer(0)} to skip GDH features.}

\item{cumulative_temp_gdd_bases}{Integer vector. Base temperatures for GDD
features. The UFEED default is \code{c(10, 7, 4, 0)}. Users may provide any
finite integer vector; it does not need to be a subset of the default. Use
\code{integer(0)} to skip GDD features.}

\item{cumulative_temp_gdh_Topt}{Numeric. Optimum temperature for GDH. The
UFEED default is 25.}

\item{cumulative_temp_gdh_Tcrit}{Numeric. Critical temperature for GDH. The
UFEED default is 36.}

\item{cumulative_temp_fill_direction}{Character. Direction passed to
\code{tidyr::fill()} for missing \code{T2M_MAX} and \code{T2M_MIN}. Valid choices are
\code{"downup"}, \code{"down"}, \code{"up"}, and \code{"updown"}. Any other
value causes an error.}

\item{cumulative_temp_message_missing}{Logical. Message when daily temperature
values are filled before hourly reconstruction.}

\item{season_max_cols}{Character vector or \code{"default"}. Base weather
variables whose EWMA/REWMA features are summarized with season-to-date
maxima. Valid choices are \code{"T2M_MAX"}, \code{"Daily_Temp_Fluctuation"},
\code{"WS2M_MAX"}, \code{"GWETROOT"}, \code{"GWETTOP"}, \code{"TSOIL1"},
\code{"TSOIL3"}, and \code{"EVPTRNS"}. With \code{"default"}, these valid
choices are automatically restricted to columns present in \code{weather_data}.
If users provide a character vector, every value must be from this list and
must be present in \code{weather_data}; otherwise the function stops with an
error.}

\item{season_min_cols}{Character vector or \code{"default"}. Base weather
variables whose EWMA/REWMA features are summarized with season-to-date
minima. Valid choices are \code{"T2M_MIN"}, \code{"Daily_Temp_Fluctuation"},
\code{"GWETROOT"}, \code{"GWETTOP"}, \code{"TSOIL1"}, \code{"TSOIL3"}, and
\code{"EVPTRNS"}. With \code{"default"}, these valid choices are automatically
restricted to columns present in \code{weather_data}. If users provide a
character vector, every value must be from this list and must be present in
\code{weather_data}; otherwise the function stops with an error.}

\item{start_filter_date}{Optional date. Rows before this date are removed after
feature computation.}

\item{end_filter_date}{Optional date. Rows after this date are removed after
feature computation.}

\item{message_progress}{Logical. Print progress messages.}
}
\value{
A data frame containing \code{Date}, \code{lon}, \code{lat}, and computed weather
features. Raw weather columns are not repeated; use \code{UFEED_wrap_up()} to
combine raw weather, computed features, and soil features.
}
\description{
Computes one or more UFEED modules from an already prepared
weather data frame. This is the main modular interface for users who want to
download weather data separately or provide local weather station data.
}
\details{
By default, this function uses the standard UFEED feature-variable sets for
either the historical workflow or the present-season workflow. The default
column sets are then intersected with the columns that are actually available
in \code{weather_data}. Users can provide a smaller subset for each feature module,
but the supplied column vectors must be subsets of the relevant UFEED default
column set.
}
\examples{
# Standard profile-specific defaults:
# features <- UFEED_compute_weather_features(weather, feature_profile = "history")

# Smaller customized subset:
# features <- UFEED_compute_weather_features(
#   weather,
#   feature_profile = "history",
#   cumsum_cols = c("ALLSKY_SFC_SW_DWN", "PRECTOTCORR"),
#   ewma_rewma_cols = c("T2M", "T2M_MAX", "T2M_MIN", "PRECTOTCORR"),
#   ewma_rewma_windows = c(7, 14, 30),
#   season_max_cols = c("T2M_MAX"),
#   season_min_cols = c("T2M_MIN")
# )

}
